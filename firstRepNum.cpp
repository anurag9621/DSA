#include<iostream>
#include<limits.h>
using namespace std;

int main(){
    int n;
    cout<<"length of an arrey is:- ";
    cin>>n;
    int a[n];
    cout<< "enter arrey's using space ' ' between two arrey:- ";

    for(int i = 0; i<n;i++)
    {
        cin>>a[i];
    }

    const int N = 1e6+2;
    int idx[N];
    for(int i = 0; i<N;i++){
        idx[i]=-1;
    }
    int minidx = INT_MAX;

    

 
    
    
}